# 排序
# lst = [1, -7, 10, 5, -5, 9, -6]
#
# result = sorted(lst, key=lambda x: abs(x))
# print(result)

# redis 使用
# from pickle import dumps
#
# from redis import Redis as _Redis

# Redis = _Redis(host='localhost', port=6379,
#               db=1, password=2125031)
#
# Redis.set('hahah', 5455)


